from setuptools import setup, find_packages

setup(
    name='semantic_correspondence',
    version='0.1.0',
    description='Semantic Correspondence with Visual Foundation Models',
    packages=find_packages(where='.'),
    python_requires='>=3.9',
)
